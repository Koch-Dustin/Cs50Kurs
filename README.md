# Cs50Kurs

Wie füge ich was hinzu?

```
ga . (git add .)
gc -m "Dein Kommentar" (git commit -m "Dein Kommentar")
gp (git push)
```

Befehle

```
 <br /> (Neue Zeile)
**Text** (Fette schrift)
*Text* (Schräge schrift)
 ```